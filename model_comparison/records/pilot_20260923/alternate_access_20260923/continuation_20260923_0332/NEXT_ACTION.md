# Waiting for the official Linux login

Run this single command in local PowerShell and finish the official browser/device flow in that terminal:

```powershell
wsl -d Ubuntu-24.04 -- /home/camus/robotgen-alt-client-20260923/codex-x86_64-unknown-linux-musl login --device-auth
```

Do not send the device code, token or key to chat. The engineering agent never read, copied or exported Windows authentication material. This is a required human authentication step, not failed model admission. No login polling process was left running by this task.

The native Linux scoped file boundary and CAD MCP checks pass. Reuse the three existing image admissions; do not call Smart AGI. After login, check only official login status, then continue the frozen serial native worker commands in plan.json, with one 1800-second attempt each and evaluation reserve before 2026-09-23 12:00 +08. Worker entrypoint is prepared but has not been live validated. Do not count its --help import check as generation.

For each stopped attempt use its safe model_final/final snapshots; move only safely inventoried artifacts to the allowed outputs path and invoke the existing pilot_evaluate.evaluate_integration path for that run_id. No operator CAD/manifest corrections. No design product or new engineering metric exists yet.
