# Concrete remaining blocker

Independent official-client ChatGPT image responses succeeded for all three fixed requested model IDs. Do not repeat admission or retry the old Smart AGI group.

Native Codex scoped filesystem isolation on this installed Windows build remains blocked: `codex sandbox -P robotgen-alt ...` exits 1 with `CreateProcessWithLogonW failed: 267`; an ordinary `:read-only` control exits 0. Owner-only temp ACLs were a hypothesis, not the demonstrated complete cause. Inherited ACLs and explicit scoped workspace roots still fail. Broad read-only access is insufficient for contestants.

The minimal CAD MCP bridge successfully invokes the existing no-network/no-credential namespace. PreToolUse guard unit checks pass, but hooks are not a complete filesystem boundary; diagnostic output also did not expose an authoritative complete native tool inventory. These checks do not permit design launch.

Needed: a working scoped native filesystem boundary or an independently verifiable supported native tool allowlist excluding host reads/execution. Global Codex config, account, system proxy, installed runtime and authentication store were not changed. No token was exported.

After the runtime capability is resolved, freeze the common Codex addendum and generation configuration, retain these three models, and use the still-unused one 1800-second attempt per model with evaluation reserve before 2026-09-23 12:00 +08. No operator-written robot CAD or manifest fixes.
